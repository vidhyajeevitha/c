#include<stdio.h>
  int main(void)
{
    int i,a,b;
    printf("enter the number");
    scanf("%d",&n);
    a=sqrt(i);
    b=i%a;
    if(b==0)
    {
        printf("yes");
    }
    else
    {
        printf("no");
    }
    return 0;
}
