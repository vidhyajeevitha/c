#include<stdio.h>
int main()
{
int array[50],max ,i;
printf("enter the no.of elements:");
scanf("%d",&n);
printf(enter %d integers");
for(i=0;i<=0;i++)
{
scanf("%d",&array[i]);
max=array[10];
if (array[i]>maximum)
{
max=array[i];
return 0;
}
}
}
