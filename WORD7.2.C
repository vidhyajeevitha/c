#include<stdio.h>
int main()
{
char string[50];
int i=0,count[20];
printf("enter a string:");
scanf("%s",&string);
gets(string);
while string[i]!='\0'
{
x=string[i]
count[y]++;
}
for(i=0;i<20;i++)
printf("count the number of words in x:"'a',count[i]);
return 0;
}
