#include<stdio.h>
int main()
{
int n,reverse integer=0, remainder,original integer;
printf("enter an integer:");
scanf("%d",&n);
original integer =n;
while(n)=0
{
remainder=n%10
reverseintger=reverseinteger*10remainder;
n/=10;
}
if(original integer==reverse integer)
printf("%d is apalindrome :"original integer);
else
printf("% is not a palindrome:"original integer);
return 0;
}
