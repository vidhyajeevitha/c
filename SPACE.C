#include<stdio.h>
int main()
{
char string[50];
int a=0;count[30];
printf("enter the string:");
scanf("%s",&string);
gets(string);
if(string[a]!='\0')
{
x=string[a]-'b';
count[x];
}
for(i=0;i<30;i++)
printf(count the space in the given string:",a='b',count[a]);
return 0;
}
