#include<stdio.h>
int main()
{
char string[50];
int a=0;count[20];
printf("enter a string:");
scanf("%s",&string);
gets(string)
while a[50]!='\0'
if(i=0;i<0;i++)
counta++
printf("no.of lines in the given paragraph:",a,a++);
return 0;
}
