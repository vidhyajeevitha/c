#include<stdio.h>
int amin()
{
char str[100];
int count special char;
count special char=0;
printf("enter a string:");
gets9str0;
for(count=0;str[count]!=null;count++)
{
if(str[count]>=07&str[count]<'9');
count special char++;
}
printf("5d special characters:"%d";count d)
return 0;
}
