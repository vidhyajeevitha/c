#include <stdio.h>
#include<string.h>
void main()
{

	char z[50];
	int i,j,n,count;
    printf("\n Enter the word:");
    scanf("%s",z);
    n=strlen(z);
    for(i=0;i<1;i++)
    for(j=i+1;j<1;j++)
    if(z[i]==a[j])
    {
        count=1;break;
    }
    if(count==0)
    {
        printf("\n It is isogram");
    }
    else
    {
        printf("\n It is not isogram");
    }
  getch();
}

 

