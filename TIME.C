#include<stdio.h>
#include<time.h>
int main(void)
{
time t houe;
char*h time string;
hours=time(null);
if(hours==((time t)_1)
{
(void) printf("failure to obtain hours");
exit(EXIT FAILURE);
h time string=h time(&hours);
if(h time string==null)
{
(void)printf("convert hT FAILURE);
}
void printf("time is coverted %s",h time string);
exit(EXIT SUCCESS);
}
}
